"use client";
import { useEffect, useState } from "react";
import { isLampRoom, movePlayer, moveToRoom } from "../game/rules";
type Position = { x: 0 | 1; y: 0 | 1 };
const rooms = [[{ name: "Spiral Stair", image: "/rooms/spiral-stair.png", description: "A narrow iron staircase curls upward through the lighthouse. Salt air slips through the seams, carrying the distant pulse of the sea." }, { name: "Lamp Room", image: "/rooms/lamp-room.png", description: "The great Fresnel lens waits in its brass housing, catching the last light of day. Beyond the glass, the horizon is turning violet." }], [{ name: "Keeper's Kitchen", image: "/rooms/keepers-kitchen.png", description: "A small cast-iron stove warms the kitchen, and a kettle hums beside a loaf of bread. Copper pans glow softly in the lantern light." }, { name: "Rocks", image: "/rooms/rocks.png", description: "Jagged rocks shine beneath the lighthouse, slick with spray and seaweed. Waves break below, leaving the air bright with salt." }]] as const;
export default function Home() { const [position, setPosition] = useState<Position>({ x: 1, y: 1 }); const [visitedKitchen, setVisitedKitchen] = useState(false); const [message, setMessage] = useState("The lighthouse waits above you."); const room = rooms[position.y][position.x]; const move = (key: string) => { const result = movePlayer(position, key, visitedKitchen); setPosition(result.position); setVisitedKitchen(result.visitedKitchen); setMessage(result.message); }; const selectRoom = (target: Position) => { const result = moveToRoom(position, target, visitedKitchen); setPosition(result.position); setVisitedKitchen(result.visitedKitchen); setMessage(result.message); }; useEffect(() => { const onKeyDown = (event: KeyboardEvent) => { if (!["ArrowUp", "ArrowRight", "ArrowDown", "ArrowLeft"].includes(event.key)) return; event.preventDefault(); move(event.key); }; window.addEventListener("keydown", onKeyDown); return () => window.removeEventListener("keydown", onKeyDown); }, [position, visitedKitchen]); return <main className="shell"><section className={`card${isLampRoom(position) ? " lamp-room" : ""}`} aria-live="polite"><p className="eyebrow">A tiny lighthouse adventure</p><h1>{room.name}</h1><img className="room-image" src={room.image} alt={`Illustration of ${room.name}`} /><p className="description">{room.description}</p><div className="map" aria-label="Lighthouse map">{rooms.flat().map((mapRoom, index) => { const x = (index % 2) as 0 | 1; const y = Math.floor(index / 2) as 0 | 1; const current = x === position.x && y === position.y; return <button key={mapRoom.name} type="button" className={`map-room${current ? " current" : ""}`} aria-current={current ? "location" : undefined} onClick={() => selectRoom({ x, y })}>{mapRoom.name}</button>; })}</div><p className="directions"><strong>Rooms</strong><span>Tap a room to travel</span></p><p className="message">{message}</p><p className="hint">Use the arrow keys or tap a room on the map.</p></section></main>; }
